class Container {
    _childs = [];
    _width = 0;
    _heigth = 0;

    constructor(childs, height, width) {
        this._childs = childs;
        this._heigth = height;
        this._width = width;

    }

    getItemsInRange(start, end) {
        // PENDING IMPLEMENTATION
    }
}